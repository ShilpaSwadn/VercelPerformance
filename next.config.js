/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed basePath to allow serving pages at root and other paths naturally
  // Removed redirects so the root page (app/page.jsx) can be displayed
};

module.exports = nextConfig;
